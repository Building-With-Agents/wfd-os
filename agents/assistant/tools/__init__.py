"""Tool definitions for conversational agents.

Each agent's tools are defined as plain Python functions decorated with metadata
so the base framework can auto-generate Gemini function declarations and dispatch
tool calls at runtime.
"""
